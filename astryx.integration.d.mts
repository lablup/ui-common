/** Types for the Astryx CLI integration manifest, so tests can import it. */
import type { AstryxIntegration } from "@astryxdesign/cli/authoring";

declare const manifest: AstryxIntegration;
export default manifest;
