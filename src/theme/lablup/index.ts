/**
 * `@lablup/ui-common/theme/lablup`: the Lablup brand theme as Astryx source.
 *
 * Pass it to `<Theme>` for runtime style injection. For pre-built CSS, import
 * `@lablup/ui-common/theme/lablup/built` together with
 * `@lablup/ui-common/theme/lablup/theme.css` instead. Use one or the other,
 * not both: they register the same theme name.
 */
export { LABLUP_ACCENT, LABLUP_INFO_TOKEN, lablupTheme } from "./lablupTheme";
