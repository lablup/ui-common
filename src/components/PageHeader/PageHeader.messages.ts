import { defineMessages } from "../../i18n/catalog";

export const pageHeaderMessages = defineMessages({
  "uic.PageHeader.retry": {
    defaultMessage: "Retry",
    description:
      "Button in the page header's error banner that retries the failed load",
  },
  "uic.PageHeader.dismissError": {
    defaultMessage: "Dismiss error",
    description: "Accessible name of the icon button that closes the error banner",
  },
});
