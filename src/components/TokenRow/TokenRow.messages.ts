import { defineMessages } from "../../i18n/catalog";

export const tokenRowMessages = defineMessages({
  "uic.TokenRow.more": {
    defaultMessage: "and {count} more",
    description:
      "After a row of tokens cut short, how many were left out; {count} is the number",
  },
});
