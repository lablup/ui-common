/**
 * EmptyState Component - Barrel Export
 *
 * The ten illustrations that used to ship here were one product's information
 * architecture: chat, models, creations, benchmark, logs, statistics,
 * schedule. No other consumer has a "creations" screen to draw for. A consumer
 * passes its own artwork through `illustration` and keeps its own vocabulary.
 */

export { EmptyState } from "./EmptyState";
export type {
  EmptyStateProps,
  EmptyStateAction,
  EmptyStateSecondaryAction,
} from "./EmptyState";
